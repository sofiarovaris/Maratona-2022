#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int t, m, n;
    cin >> t;
    cin >> m >> n;
    vector<int, int>

    for(int i=0; i<m; i++){
        for(int j=0; j<m; j++){
            cin >> 
        }
    }

    return 0;
}